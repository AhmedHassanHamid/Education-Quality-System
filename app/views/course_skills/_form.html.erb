<%= form_with(model: course_skill) do |form| %>
  <% if course_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_skill.errors.count, "error") %> prohibited this course_skill from being saved:</h2>

      <ul>
        <% course_skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_code %>
    <%= form.text_field :course_code %>
  </div>

  <div class="field">
    <%= form.label :skills %>
    <%= form.text_field :skills %>
  </div>

  <div class="field">
    <%= form.label :User_id %>
    <%= form.text_field :User_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
